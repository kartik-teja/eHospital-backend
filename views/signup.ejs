<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Reports</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"
        integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js"
        integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js"
        integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl"
        crossorigin="anonymous"></script>
    <style>
        .highlight {
            background-color: #f8f9fa;
        }

        .overlay.active {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1;
        }
    </style>
</head>

<body>
    <div id="overlay" class="overlay"></div>
    <div class="container">
        <div class="row py-5 position-relative">
            <div class="col-6 text-center" id="doctor-section">
                <div id="doctor-text" class="h-auto">Doctor Sign Up</div>
                <form id="doctor-form" class="w-75 p-3 border rounded bg-light mx-auto d-none">
                    <h4>Doctor Sign Up</h4>
                    <div class="mb-3">
                        <label for="doctor-name" class="form-label">Full Name</label>
                        <input id="doctor-name" type="text" class="form-control" placeholder="Full Name">
                    </div>
                    <div class="mb-3">
                        <label for="doctor-username" class="form-label">Username</label>
                        <input id="doctor-username" type="text" class="form-control" placeholder="Username">
                    </div>
                    <div class="mb-3">
                        <label for="doctor-password" class="form-label">Password</label>
                        <input id="doctor-password" type="password" class="form-control" placeholder="********">
                    </div>
                    <div class="mb-3">
                        <label for="doctor-phone" class="form-label">Phone Number</label>
                        <input id="doctor-phone" type="text" class="form-control" placeholder="Phone Number">
                    </div>
                    <div class="mb-3">
                        <label for="doctor-email" class="form-label">Email</label>
                        <input id="doctor-email" type="email" class="form-control" placeholder="Email">
                    </div>
                </form>
            </div>
            <div class="col-6 text-center" id="patient-section">
                <div id="patient-text" class="h-auto">Patient Sign Up</div>
                <form id="patient-form" class="w-75 p-3 border rounded bg-light mx-auto d-none">
                    <h4>Patient Sign Up</h4>
                    <div class="mb-3">
                        <label for="patient-name" class="form-label">Full Name</label>
                        <input id="patient-name" type="text" class="form-control" placeholder="Full Name">
                    </div>
                    <div class="mb-3">
                        <label for="patient-username" class="form-label">Username</label>
                        <input id="patient-username" type="text" class="form-control" placeholder="Username">
                    </div>
                    <div class="mb-3">
                        <label for="patient-password" class="form-label">Password</label>
                        <input id="patient-password" type="password" class="form-control" placeholder="********">
                    </div>
                    <div class="mb-3">
                        <label for="patient-phone" class="form-label">Phone Number</label>
                        <input id="patient-phone" type="text" class="form-control" placeholder="Phone Number">
                    </div>
                    <div class="mb-3">
                        <label for="patient-email" class="form-label">Email</label>
                        <input id="patient-email" type="email" class="form-control" placeholder="Email">
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const doctorSection = document.getElementById('doctor-section');
            const patientSection = document.getElementById('patient-section');
            const doctorForm = document.getElementById('doctor-form');
            const patientForm = document.getElementById('patient-form');
            const doctorText = document.getElementById('doctor-text');
            const patientText = document.getElementById('patient-text');
            const overlay = document.getElementById('overlay');

            doctorSection.addEventListener('mouseenter', () => {
                doctorForm.classList.remove('d-none');
                doctorText.classList.add('d-none');
                doctorSection.classList.add('highlight');
                overlay.classList.add('active');
            });

            doctorSection.addEventListener('mouseleave', () => {
                doctorForm.classList.add('d-none');
                doctorText.classList.remove('d-none');
                doctorSection.classList.remove('highlight');
                overlay.classList.remove('active');
            });

            patientSection.addEventListener('mouseenter', () => {
                patientForm.classList.remove('d-none');
                patientText.classList.add('d-none');
                patientSection.classList.add('highlight');
                overlay.classList.add('active');
            });

            patientSection.addEventListener('mouseleave', () => {
                patientForm.classList.add('d-none');
                patientText.classList.remove('d-none');
                patientSection.classList.remove('highlight');
                overlay.classList.remove('active');
            });
        });
    </script>
</body>

</html>